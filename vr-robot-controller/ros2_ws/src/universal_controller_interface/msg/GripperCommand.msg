# Reserved for future robot adapters with a gripper/arm (not used by the
# OSOYOO car). Kept in the shared contract now so the VR client and
# universal_controller_bridge don't need to change when an arm is added.
#
# TODO: define once the first arm adapter is built.

std_msgs/Header header
